version: 2.1

orbs:
  aws-cli: circleci/aws-cli@2.0.3

jobs:
  ansible:
    docker:
      - image: python:3.7-alpine3.11
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "53:7e:7d:cb:cc:d9:a6:f5:07:7d:21:75:89:49:ef:14"
      - run: apk add --update ansible
      - run: ansible-playbook main-remote.yml -i inventory

workflows:
  ansible-workflow:
    jobs:
      - ansible
